# Motorhome

## Auto-merge

This repository includes a GitHub Actions workflow at `.github/workflows/automerge.yml` that enables auto-merge for pull requests labeled `automerge`.

### How it works

1. Open a pull request.
2. Add the `automerge` label.
3. The workflow enables **squash auto-merge** (once required checks and branch protections pass).
